<%= form_for(event) do |f| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_name %>
    <%= f.text_field :event_name %>
  </div>

  <div class="field">
    <%= f.label :event_date %>
    <%= f.date_select :event_date %>
  </div>

  <div class="field">
    <%= f.label :event_time %>
    <%= f.time_select :event_time %>
  </div>

  <div class="field">
    <%= f.label :event_location %>
    <%= f.text_field :event_location %>
  </div>

    <div class="field">
    <%= f.label :map %>
    <%= f.text_field :map %>
  </div>

  <div class="field">
    <%= f.label :event_content %>
    <%= f.text_area :event_content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
